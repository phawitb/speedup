{
  "name": "speakup-analysis-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
